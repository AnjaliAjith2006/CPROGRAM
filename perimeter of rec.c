#include <stdio.h>
int main()
{
    int l,b;
    printf("enter the value of length(l) and breadth(b)");
    scanf("%d %d",&l,&b);
    printf("perimeter of rectangle is =%d",2*(l+b));
    return 0;

}